[project]
name = "feiertage"
version = "1.2.2"
description = "Feiertage API - German and Austrian public holidays"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --cov=app --cov-report=term-missing"
